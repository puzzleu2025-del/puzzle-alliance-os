# 拼圖聯盟 OS：管理功能與管理員登入迭代

## 本次完成

- 活動：建立時可填甘特基準日、活動執行起訖、類型、規模、目標人數、總召、代理、場地、工作組、預算與本週里程碑。詳情依參考工作簿使用 P1–P9、活動前 8 週至活動後 2 週，顯示完成率、逾期與下一任務。手機只顯示目前階段及前後各一階段。
- 任務：選擇活動與 P1–P9 階段後會依活動日自動帶入建議開始日和 deadline，兩個日期都可再調整；儲存後 deadline 直接出現在行事曆。另可填主責、代理、協作、完成標準、工時、完成率與前置任務。
- 行事曆：點日期可直接新增活動、任務或會議，資料寫回同一工作空間；既有項目仍可改期，活動與任務會連同工作區間平移，會議保留原時長。
- 會議：可填類型、主持、記錄、開始／結束時間、地點、線上連結、出席者與議程；卡片完整顯示起訖與連結，可逐人更新出席回覆；確認會議不會覆寫既有回覆。
- 組織與角色：說明系統角色和活動角色的差別、責任邊界，以及目前活動總召／代理與任務主責指派。
- 管理員安全：唯一管理員可設定站內密碼、以 email＋密碼登入、改密碼與登出。密碼使用 PBKDF2-SHA256 600,000 次及獨立 salt；session cookie 為 HttpOnly，資料庫只存 token hash；具 CSRF 檢查及依來源與帳號分桶的登入節流。

## 如何登入及設定／修改密碼

正式功能入口是完整應用程式（目前本機為 `http://localhost:5173/`），不是 `127.0.0.1:5174` 的純靜態互動預覽。

1. 首次啟用時，按「首次啟用：以 ChatGPT 驗證身分」。首位通過可信驗證的使用者會成為唯一系統管理員。
2. 進入「系統設定」→「系統管理員帳號」。
3. 第一次輸入兩次新密碼（至少 12 個字元）並儲存。
4. 後續可在登入頁使用該管理員的 email 與站內密碼登入。
5. 修改密碼仍在同一區塊，必須輸入目前密碼。成功後其他帳密 session 會被撤銷。
6. 「登出此裝置」會清除本站 session；若本次由 ChatGPT 登入，也會繼續完成 ChatGPT 登出流程。

本機開發環境只使用迴圈位址內的開發身分，不是正式帳號。正式後端部署後會使用首次通過可信驗證的管理員身分；系統不在程式或文件內放置預設密碼。

## 部署界線

GitHub Pages 只能承載靜態前端，無法安全保存密碼、session 或跨裝置資料。本專案已備妥 Pages 互動預覽，但真正管理員登入、改密碼及跨裝置共享資料仍需部署 API 與 D1 資料庫。使用者目前尚未建立 GitHub 帳號，因此沒有執行公開發布。

正式環境也必須確保 `oai-authenticated-*` 標頭只能由可信的 ChatGPT／Sites gateway 注入，不能讓公開客戶端自行提供這些標頭。
